{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "npx lerna run lint",
    "test": "npx lerna run test",
    "build": "npx lerna run build"
  },
  "devDependencies": {
    "lerna": "^7.1.4",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  }
}
